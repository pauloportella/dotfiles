- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/karabiner: karabiner
    ~/.hammerspoon: hammerspoon
    ~/.config/fish: config/fish
    ~/.config/fisher: config/fisher
    ~/.config/omf: config/omf
    ~/.config/tabtab: config/tabtab


- create:
    # - ~/downloads

- shell:
  - [bash ./.macos, Running mac os script]
